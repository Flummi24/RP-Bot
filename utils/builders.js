const { EmbedBuilder, ActionRowBuilder, ContainerBuilder, TextDisplayBuilder, MediaGalleryBuilder,
 } = require("discord.js")

async function container(title, content) {
    
}

async function embed(title, content) {
    
}